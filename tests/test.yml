---
- hosts: all
  remote_user: root

  roles:
    - ansible-role-httpd

  tasks:
    - name: test if port is open
      wait_for:
        host: "{{ inventory_hostname }}"
        port: 80
        delay: 5
        timeout: 30

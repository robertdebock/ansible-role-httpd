---
- hosts: localhost
  remote_user: root

  roles:
    - httpd

  tasks:
    - name: test if port is open
      wait_for:
        host: localhost
        port: 80
        delay: 5
        timeout: 30
